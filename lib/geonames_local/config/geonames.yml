#
# Geonames Local Config Example
#
:store: mongodb # soon pg too?
:level: city # hood, city, state, nation
:nations: [br, ar] #, us] # which nations to include
:locales: [pt, en, es] # which locales to include
:min_pop: 7_000 # minimum population for cities
:mapping:
  :name: name
  :geom: true
:db:
  :host: localhost:28017
  :name: geonames_local
  # :user: MONGO_USERNAME
  # :password: MONGO_PASSWORD
  # :clean: true # defaults to false
