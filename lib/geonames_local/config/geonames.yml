#
# Geonames Local Config Example
#
:store: mongodb
:level: city
:nations: [br, us] # [] to populate nations database
:locales: [pt, en]
:mapping:
  :name: name
  :geom: true
:db:
  :host: localhost
  :name: geonames_local
  # :user:
  # :password:
  # :purge: false
