#
# Geonames Local Config Example
#
:store: mongodb
:codes: [br] # [] to populate nations
:level: city
:mapping:
  :name: name
  :geom: true
:db:
  :host: localhost
  :dbname: geonames_local
  # :user:
  # :password:
  # :purge: false
