#
# Geonames Local Config Example
#
# Stores: postgres, mongodb, tokyo, redis
:store: postgres
:codes: [br, it]
:level: city
:mapping:
  :name: name
  :geom: true
:db:
  :host: localhost
  :dbname: dbname
  :user: postgres
  :password:
