#
# Geonames Local Config Example
#
store: mongodb
level: city
nations: [br] #, us] #, it, nl] pa, et, pe
locales: [pt, en, es]
min_pop: 10
mapping:
  name: name
  geom: true
db:
  host: localhost
  name: geonames_local
  user:
  pass:
  purge: false
