#
# Geonames Local Config Example
#
:store: mongodb # postgis / tokyo
:codes: [br] # [] to store all nations informations (not recursive)
:level: city
:min_pop: 100000
:mapping:
  :name: name
  :geom: true
:db:
  :host: localhost
  :name: baccafe_development
  :user:
  :pass:
  :purge: false
